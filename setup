#!/usr/bin/env bash

set -u

## Globals ##
RECIPE_DIR="${RECIPE_DIR:-./recipes}"
TARGET_RECIPE_NAME="${1:-${TARGET_RECIPE_NAME:-}}"

## functions ##
source scripts/common.sh

## Main ##
# Load recipe
if [ -n "$TARGET_RECIPE_NAME" ]; then
    if [[ ! "$TARGET_RECIPE_NAME" =~ ^[A-Za-z0-9_-]+$ ]]; then
        echo_error "Invalid recipe name: '$TARGET_RECIPE_NAME'"
        exit 1
    fi

    echo_info "Using specified target recipe: '$TARGET_RECIPE_NAME'"
    recipe_to_source="$TARGET_RECIPE_NAME"
else
    # Platform
    KERNEL_NAME="$(uname -s)"
    CODESPACES="${CODESPACES:-false}"

    case $KERNEL_NAME in
        Darwin)
            platform_recipe="macosx"
            ;;
        Linux)
            if "$CODESPACES"; then
                platform_recipe="codespaces"
            else
                platform_recipe="linux"
            fi
            ;;
        *)
            echo_error "Unable to detect platform: $KERNEL_NAME"
            exit 1
            ;;
    esac

    echo_info "Using automatically chosen recipe: '$platform_recipe'"
    recipe_to_source="$platform_recipe"
fi

if ! source_recipe "$recipe_to_source"; then
    echo_error "Failed to source recipe '$recipe_to_source'"
    exit 1
fi

# sudo
if "${SUDO_UP_FRONT}"; then
    echo_info "Requesting sudo password to cache credentials for script run duration"
    sudo_up_front
fi

# zsh
if "${SETUP_ZSH}"; then
    echo_info "Configure zsh"

    mkdir -p "$HOME/.zshrc.d"
    mkdir -p "$HOME/.local/share/zsh/completions"

    if "${SETUP_OH_MY_ZSH}"; then
        echo_info "Install oh-my-zsh"
        ./scripts/oh-my-zsh.sh install
    fi
fi

# hashicorp
if "${SETUP_HASHI_APT_REPO}"; then
    echo_info "Configure the HashiCorp apt repository"
    ./scripts/hashi_apt_repo.sh configure
fi

# apt
if "${SETUP_APT}"; then
    echo_info "Install / remove / upgrade apt packages"

    if [ -n "${APT_INSTALL_PACKAGES[*]:-}" ]; then
        ./scripts/apt.sh install "${APT_INSTALL_PACKAGES[@]}"
    fi

    if [ -n "${APT_REMOVE_PACKAGES[*]:-}" ]; then
        ./scripts/apt.sh remove "${APT_REMOVE_PACKAGES[@]}"
    fi

    if "${APT_UPGRADE}"; then
        ./scripts/apt.sh upgrade
    fi

    if "${APT_CLEAN}"; then
        ./scripts/apt.sh clean
    fi
fi

# homebrew
if "${SETUP_HOMEBREW}"; then
    echo_info "Install and configure homebrew"

    ./scripts/homebrew.sh install
    eval "$(/opt/homebrew/bin/brew shellenv)"

    if [ -f "${HOMEBREW_BUNDLE_FILE:-}" ]; then
        echo_info "Install homebrew bundle"
        ./scripts/homebrew.sh bundle
    fi
fi

# stow
if [ -n "${STOW_PACKAGES[*]:-}" ]; then
    echo_info "Install requested dotfiles packages via stow"
    ./scripts/stow.sh install "${STOW_PACKAGES[@]}"
fi

# atuin
if "${SETUP_ATUIN}"; then
    echo_info "Setting up atuin"
    ./scripts/atuin.sh install
fi

# nvim
if "${SETUP_NVIM}"; then
    echo_info "Installing the latest nvim binary release"
    ./scripts/neovim.sh install
fi

# yq
if "${SETUP_YQ}"; then
    echo_info "Installing yq"
    ./scripts/yq.sh install
fi

if "${SETUP_ASTRAL_SH_TOOLS}"; then
    echo_info "Installing astral sh tooling"
    ./scripts/astral.sh install
fi

if "${SETUP_CODEX_CLI}"; then
    echo_info "Installing codex cli"
    ./scripts/codex.sh install
fi

# macos
if "${SETUP_MACOS_SYSTEM}"; then
    if [ ! "${SET_MACOS_HOSTNAME}" == "None" ]; then
        echo_info "Set macOS hostname to: ${SET_MACOS_HOSTNAME}"
        ./scripts/macos.sh hostname "${SET_MACOS_HOSTNAME}"
    fi

    echo_info "Configure macOS system settings"
    ./scripts/macos.sh configure
fi
